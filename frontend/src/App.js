import React from 'react';
import Startup from './pages/Startup'; // Adjust the path if necessary

const App = () => {
    return (
        <div>
            <Startup />
        </div>
    );
};

export default App;
